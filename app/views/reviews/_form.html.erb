<%= form_for @review, remote: true do |f| %>
  <%= f.hidden_field :place_id, input_html: { :value => @place_id } %>
  <%= f.text_field :content, class: "form-control" %>
  <%= render 'rating', name: 'english_rating' %>

  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>

